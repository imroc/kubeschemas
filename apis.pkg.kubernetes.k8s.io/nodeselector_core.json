{
  "description": "NodeSelector represents the union of the results of one or more label queries over a set of nodes; that is, it represents the OR of the selectors represented by the node selector terms.",
  "properties": {
    "NodeSelectorTerms": {
      "description": "Required. A list of node selector terms. The terms are ORed.",
      "items": {
        "$ref": "../apis.pkg.kubernetes.k8s.io/nodeselectorterm_core.json",
        "default": {}
      },
      "type": "array"
    }
  },
  "required": [
    "NodeSelectorTerms"
  ],
  "type": "object"
}