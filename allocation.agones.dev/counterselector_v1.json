{
  "description": "CounterSelector is the filter options for a GameServer based on the count and/or available capacity.",
  "properties": {
    "maxAvailable": {
      "default": 0,
      "description": "MaxAvailable specifies the maximum capacity (current capacity - current count) available on a GameServer. Defaults to 0, which translates to max(int64).",
      "format": "int64",
      "type": "integer"
    },
    "maxCount": {
      "default": 0,
      "description": "MaxCount is the maximum current value. Defaults to 0, which translates as max(in64).",
      "format": "int64",
      "type": "integer"
    },
    "minAvailable": {
      "default": 0,
      "description": "MinAvailable specifies the minimum capacity (current capacity - current count) available on a GameServer. Defaults to 0.",
      "format": "int64",
      "type": "integer"
    },
    "minCount": {
      "default": 0,
      "description": "MinCount is the minimum current value. Defaults to 0.",
      "format": "int64",
      "type": "integer"
    }
  },
  "type": "object"
}