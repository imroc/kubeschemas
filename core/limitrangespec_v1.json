{
  "description": "LimitRangeSpec defines a min/max usage limit for resources that match on kind.",
  "properties": {
    "limits": {
      "description": "Limits is the list of LimitRangeItem objects that are enforced.",
      "items": {
        "allOf": [
          {
            "$ref": "../core/limitrangeitem_v1.json"
          }
        ],
        "default": {}
      },
      "type": "array"
    }
  },
  "required": [
    "limits"
  ],
  "type": "object"
}